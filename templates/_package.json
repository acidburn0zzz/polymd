{
  "name": "ELEMENT-NAME",
  "version": "ELEMENT-VERSION",
  "license": "Apache-2.0",
  "description": "ELEMENT-DESCRIPTION",
  "devDependencies": {
    "polymer-cli": "^1.2.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": false,
  "main": [
    "ELEMENT-NAME.html"
  ],
  "author": "ELEMENT-AUTHOR",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "lint": "node_modules/.bin/polymer lint ELEMENT-NAME.html --rules polymer-1",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"ELEMENT-NAME:local-test\"",
    "test": "node_modules/.bin/polymer test --plugin local",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/REPOSITORY-NAME.git"
  },
  "bugs": {
    "url": "https://github.com/REPOSITORY-NAME/issues"
  }
}
